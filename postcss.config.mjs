// postcss.config.mjs
const config = {
  plugins: {
    "@tailwindcss/postcss": {}, // empty object = default options
    // autoprefixer: {},         // add if you need it explicitly
  },
};

export default config;
